//var test = 1 + 2;

//while(test >= 0) {
//    print test;
//    test = test - 1;
//}

//for (var i = 0; i < 3; i = i + 1) {
//    print i;
//}

//fun sum(a, b, c) {
//    return a + b + c;
//}

//print sum(1, 2, 3);

// print clock();

//fun fib(n) {
//    if (n < 2) return n;
//    return fib(n - 2) + fib(n - 1);
//}

//var start = clock();
//print fib(35);
//print clock() - start;

fun outer() {
    var x = "outside";
    fun inner() {
        print x;
    }

    inner();
}

outer();

